# El esquema SQL no debe ser accesible por web (el instalador lo lee por
# sistema de archivos, no por HTTP). Esto no afecta la lectura desde PHP.
Require all denied

<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
